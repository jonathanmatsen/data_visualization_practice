# into and set up data
import pandas as pd

reviews = pd.read_csv('fandango_scores.csv')
norm_reviews_cols = ['FILM', 'RT_user_norm', 'Metacritic_user_nom', 'IMDB_norm', 'Fandango_Ratingvalue', 'Fandango_Stars']
norm_reviews = reviews[norm_reviews_cols]

print(norm_reviews.head(1))
